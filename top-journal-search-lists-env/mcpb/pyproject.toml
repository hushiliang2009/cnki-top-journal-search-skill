[project]
name = "cnki-search-env-mcp"
version = "0.1.0"
description = "Public CNKI theme-search MCP server"
requires-python = ">=3.11"
dependencies = [
  "mcp>=1,<2",
  "playwright>=1.45,<2",
]

[tool.uv]
package = false
