# CNKI v0.5.2 and Environment v0.3.2 Release Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Audit, repair, verify, and release the general Skill v0.5.2 and environment Skill v0.3.2.

**Architecture:** Preserve both catalog snapshots and every search-priority sequence byte-for-byte. Limit changes to the professional-search rules, release metadata, mirrored runtime files, tests, and generated release artifacts.

**Tech Stack:** Python 3.11+, pytest, ruff, mypy, Playwright, MCPB, GitHub Actions, GitHub Releases.

## Global Constraints

- Do not change the general ten-level catalog, group membership, journal counts, or priority order.
- Do not change the environment twelve-level catalog, group membership, journal counts, or priority order.
- Keep `scripts/` and `mcpb/src/` runtime mirrors byte-identical.
- Treat `SU, TKA, TI, KY, AB, CO, FT, AU, FI, RP, AF, LY, RF, FU, CLC, SN, CN, DOI, QKLM, FAF, CF` as the complete CNKI professional-search field whitelist.
- Keep `YE` unsupported and apply years only through the publication-year controls.

---

### Task 1: Repair release-blocking static checks

**Files:**
- Modify: `top-journal-search-lists/scripts/cnki_search/professional.py`
- Modify: `top-journal-search-lists/scripts/cnki_search/webvpn.py`
- Modify: `top-journal-search-lists-env/scripts/cnki_search_env/professional.py`
- Modify: `top-journal-search-lists-env/scripts/cnki_search_env/webvpn.py`
- Modify: corresponding `mcpb/src/` mirrors

- [x] Reproduce the mypy failures for optional year endpoints.
- [x] Add the minimum explicit non-`None` narrowing required by mypy.
- [x] Run ruff, mypy, and focused professional-search tests.

### Task 2: Audit immutable catalog and priority contracts

**Files:**
- Test: both catalog validators and catalog regression suites

- [x] Record pre-release hashes for catalog snapshots and machine-readable companions.
- [x] Validate the general ten-level order and environment twelve-level order.
- [x] Confirm no catalog or priority file appears in the release diff.

### Task 3: Bump release metadata

**Files:**
- Modify: version-bearing manifests, package metadata, README files, tests, and release baselines discovered by repository search

- [x] Change general version from `0.5.1` to `0.5.2`.
- [x] Change environment version from `0.3.1` to `0.3.2`.
- [x] Run version-consistency and package-contract tests.

### Task 4: Build and verify release artifacts

**Files:**
- Generate: release archives and `checksums.sha256` in an isolated output directory

- [x] Run both full test suites in separate Python processes.
- [x] Run Skill structure, catalog, ruff, mypy, handshake, and build checks.
- [x] Rebuild artifacts twice and compare extracted content and checksums.
- [x] Install or inspect built packages in isolated locations and run smoke tests.

### Task 5: Integrate and publish

**Files:**
- Update: PR metadata and GitHub Release records

- [ ] Push the release commit and require all PR checks to pass.
- [ ] Merge PR 24 into `main` without force-push.
- [ ] Create tags `v0.5.2` and `top-journal-search-lists-env-v0.3.2` from the verified main commit.
- [ ] Publish both GitHub releases with their corresponding assets and checksums.
- [ ] Download published assets and verify hashes and archive readability.
