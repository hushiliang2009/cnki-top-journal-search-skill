[project]
name = "cnki-search-mcp"
version = "0.1.0"
description = "Visible-browser CNKI MCP server"
requires-python = ">=3.11"
dependencies = [
  "mcp>=1.0.0,<2",
  "playwright>=1.45,<2",
]

[tool.uv]
package = false
